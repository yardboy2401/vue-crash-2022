{
  "name": "vue-crash-2022",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "serve -s dist && json-server --watch db.json --port 5000",
    "develop": "concurrently \"npm run serve\" \"npm run backend\"",
    "serve": "serve -s dist",
    "build": "vue-cli-service build",
    "backend": "json-server --watch db.json --port 5000"
  },
  "dependencies": {
    "core-js": "^3.8.3",
    "serve": "^13.0.2",
    "vue": "^3.2.13",
    "vue-router": "^4.0.13"
  },
  "devDependencies": {
    "@vue/cli-plugin-babel": "~5.0.0",
    "@vue/cli-service": "~5.0.0",
    "concurrently": "^7.0.0"
  }
}
